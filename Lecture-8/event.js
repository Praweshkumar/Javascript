                           //  Events in js..//

/* Events==> The change in the state of an object is known as Event
             Events are fixed to notify code of "intersting changes " that may affect code execution.

  1) Mouse events(click,double click etc.)
  2) keyboard events(keypres,keyup,keydown)
  3) Form events(Submit etc.)
  4) Print event & many more           

  ** Event handling in js..**
   node.event=()=>{
    //handle here
   }

  important points==> inline and js file you are doing event handling priority will given js file handle.

  ** Event object in js...** it is a special object that has details about the event.
     All event handlers have access to the Event Objects's properties and methods.
     node.event-(e)=>{
        // handle here 
     }
      e.target,e.type,e.clientX,e.clientY

  ** Event listeners...**
  
  node.addEventListener(event,callback)
  node.removeEventListener(event,callback)
  Note==>the callback reference should be same to remove

*/

























